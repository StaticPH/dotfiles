##!/bin/bash
complete -W "remove autoremove install update download upgrade downgrade info show cleanup list search refresh check package-changelog search-installed owner-of who-owns list-package-owned list-orphans list-outdated list-installed list-explicitly-installed list-dependencies provides-what help --completion" pac
